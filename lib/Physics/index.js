/**
 * Namespace contains physics methods, constants, and formulas.
 * @namespace Physics
 */
const Physics = {
  /**
   * @property {number} FRICTION The friction constant.
   */
  FRICTION: 30,
  /**
   * Updates the object position relying on its current controls
   * and config.
   * @param {PhysicsObject} obj `PhysicsObject` instance
   * @param {number} delta Time delta
   */
  update: function(obj, delta) {
    // a lot of code here
  },
};

module.exports = Physics;
